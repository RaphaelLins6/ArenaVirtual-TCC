<ContentPage
    x:Class="ArenaVirtual.Views.Atleta.ProcurarTimesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Procurar Times">

    <CollectionView ItemsSource="{Binding Times}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="10" Padding="12">
                    <VerticalStackLayout>
                        <Label
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="{Binding Nome}" />
                        <Label FontSize="14" Text="{Binding Descricao}" />
                        <Button
                            Command="{Binding BindingContext.EntrarCommand, Source={x:Reference NomeDaPagina}}"
                            CommandParameter="{Binding .}"
                            Text="Entrar no Time" />
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
